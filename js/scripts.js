// scripts.js

var a = 12,
    b = -23,
    value = (a * a) - (2 * a * b) + (b * b);
console.log(value);

if (value > 0 && value != 0 ) {
	console.log('Wynik dodatni');
} else if (value < 0 && value != 0 ) {
	console.log('Wynik dodatni');
} else {
	console.log('Wynik równy 0');
}
